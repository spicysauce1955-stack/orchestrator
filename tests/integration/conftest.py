"""Shared helpers for integration tests that drive stdio JSON-RPC subprocesses."""
# Helpers are in _rpc_helpers.py (importable as a package module).
# This conftest.py exists as the integration-test conftest marker.
