
# Brainstorm visual-companion scratch
.superpowers/

# Python artifacts
__pycache__/
*.py[cod]
*.pyo
.venv/
*.egg-info/
dist/
build/

# Temporary agent worktrees created by the executor
.worktrees/

# Per-repo run state: checkpoint + span SQLite DBs (never GC'd, not source)
.orch/
